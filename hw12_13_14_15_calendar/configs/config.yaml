logger:
  level: "info"

server:
  host: 127.0.0.1
  port: 1985

db:
  type: "sql"
  sql:
    driver: "pgx"
    name: "calendar"
    user: "postgres"
    password: "postgres"
    host: "localhost"
    port: "5432"
